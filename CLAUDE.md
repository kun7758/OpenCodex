# CLAUDE.md

The canonical repository instructions are maintained in [AGENTS.md](AGENTS.md). Read and follow that file before changing OpenNova.

Current baseline: OpenNova 0.4.3 uses the Textual TUI as its only interactive interface. Do not reintroduce or document the removed line-oriented interactive CLI or the obsolete `opennova tui` command. Use `uv run opennova` to launch the application.
