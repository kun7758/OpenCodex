## 怎么配置mcp

`runtime/agent.py _run_act_mode() await self._ensure_mcp_ready()` 

## runtime/loop.py ReActLoop.run()

### 1. 怎么注入的工具？

### 2. 怎么判断走 Plan 还是 Act？

这意味着第一次循环不需要模型再次决定，直接进入计划模式。什么意思？

pending_routed_action对象是走 Plan 时的一次循环吗？

response = await self._think()是在干啥？